# Core (tested with Python 3.10-3.12)
numpy>=1.24
opencv-python>=4.8,<5     # MUST be 4.x: OpenCV 5.0 removed HOGDescriptor and the Darknet (YOLO) loader
                          # (use opencv-python-headless>=4.8,<5 on servers / Colab)
scipy>=1.10
scikit-image>=0.21        # only for cross-checking our PSNR/SSIM in tests
PyWavelets>=1.4           # wavelet codec
pandas>=2.0
matplotlib>=3.7
tabulate>=0.9             # markdown tables
jinja2>=3.1               # LaTeX table export (pandas.to_latex)
pytest>=7
psutil>=5.9               # cross-platform memory measurement (exp7, Windows has no 'resource')

# Optional stronger detector (pulls in PyTorch, ~2 GB):
# ultralytics>=8.3
